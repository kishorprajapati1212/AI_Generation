In this project i make the AI generation Webapplication using the stable difusion api

Where user can generate the Images and Video by prompt and image input as well as it also have the credit system.

first step to clone the repo

then write cd server
-- npm start

then gi the client 
-- npm run dev
